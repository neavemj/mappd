"""
Assembly rules

The inputs are:
    - trimmed reads in fastq format
The outputs are:
    - assembled contigs in fasta format
"""

rule spades:
    input:
        "subsampled/{sample}.fastq"
    output:
        directory("spades/{sample}")
    log:
        "logs/spades/{sample}.log"
    shell:
        "spades.py -o {output} -s {input} > {log}"